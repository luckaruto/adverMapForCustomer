import React from "react";

export default function DetailOfAdvertisement({
  format,
  width,
  height,
  img_url,
  content,
  created_at,
  updated_at,
  type,
  formatspace,
  address,
  className,
}) {
  return <div>DetailOfAdvertisement</div>;
}
